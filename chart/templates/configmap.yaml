apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "s3-viewer.fullname" . }}
  labels:
    {{- include "s3-viewer.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
